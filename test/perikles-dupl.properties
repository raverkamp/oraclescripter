# this will trigger an exception because of duplicte files

directory=/tmp/script-perikles
usegit=true
object-where=1=1
private-synonyms=true
sequences=true

suffix.package_body=package
suffix.package_spec=package

